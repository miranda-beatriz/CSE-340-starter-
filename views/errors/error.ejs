<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        /* Container que contém a imagem de fundo */
        .error-page {
            position: relative;
            width: 50%;
            height: 50vh; /* Garante que ocupe toda a tela */
            background: url('/images/vehicles/crashedcar.jpg') no-repeat center center;
            background-size: cover;
            display: flex;
            justify-content: flex-start; /* Alinha à esquerda */
            align-items: flex-start; /* Posiciona o conteúdo no topo */
            padding: 20px;
            box-sizing: border-box;
        }

        /* Caixa semi-transparente para a mensagem */
        .error-container {
            background: rgba(0, 0, 0, 0.274);
            padding: 30px;
            border-radius: 10px;
            text-align: left; /* Alinha o texto à esquerda */
            color: white;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            max-width: 100%;
            margin-top: -17px; /* Adiciona um espaço do topo */
            height: 485px;
            width: 2300px;
        }

        h1 {
            margin-bottom: 15px;
            font-size: 2em;
        }

        p {
            font-size: 1.2em;
        }

        /* Estilos específicos para celulares */
        @media (max-width: 768px) {
            .error-container {
                max-width: 95%;
                padding: 15px;
            }

            h1 {
                font-size: 1.5em;
            }

            p {
                font-size: 1em;
            }
        }
    </style>
</head>
<body>
    <div class="error-page">
        <div class="error-container">
            <h1>Erro <%= status %></h1>
            <p><%= message %></p>
        </div>
    </div>
</body>
</html>
